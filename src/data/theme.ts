import type { Theme } from "@/types/tailwind";

const theme: Theme = {
  colors: {
    primary: "orange",
    blur: {
      top: "orange",
      bottom: "violet",
    },
  },
};

export default theme;
